<?php
/** @var \Zwernemann\Withdrawal\Block\Guest\Search $block */
?>
<div class="container mx-auto py-6 max-w-md">
    <div class="bg-white rounded shadow p-6">
        <h2 class="text-xl font-semibold mb-4">
            <?= $block->escapeHtml(__('Search Order for Withdrawal')) ?>
        </h2>
        <p class="text-gray-600 mb-6">
            <?= $block->escapeHtml(__('Please enter your order number and the email address used for the order to find your order.')) ?>
        </p>
        <form method="post"
              action="<?= $block->escapeUrl($block->getFormAction()) ?>"
              id="withdrawal-guest-search-form"
              x-data="{ submitted: false }"
              @submit="submitted = true"
              novalidate>
            <?= /* @noEscape */ $block->getBlockHtml('formkey') ?>

            <div class="mb-4">
                <label for="order_increment_id" class="block text-sm font-medium text-gray-700 mb-1">
                    <?= $block->escapeHtml(__('Order Number')) ?>
                    <span class="text-red-500">*</span>
                </label>
                <input type="text"
                       name="order_increment_id"
                       id="order_increment_id"
                       required
                       class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
                       placeholder="<?= $block->escapeHtmlAttr(__('e.g. 000000001')) ?>"
                       autocomplete="off"/>
            </div>

            <div class="mb-6">
                <label for="email" class="block text-sm font-medium text-gray-700 mb-1">
                    <?= $block->escapeHtml(__('Email Address')) ?>
                    <span class="text-red-500">*</span>
                </label>
                <input type="email"
                       name="email"
                       id="email"
                       required
                       class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
                       placeholder="<?= $block->escapeHtmlAttr(__('your@email.com')) ?>"
                       autocomplete="email"/>
            </div>

            <button type="submit"
                    class="btn btn-primary w-full"
                    :disabled="submitted"
                    title="<?= $block->escapeHtmlAttr(__('Search Order')) ?>">
                <span><?= $block->escapeHtml(__('Search Order')) ?></span>
            </button>
        </form>
    </div>
</div>
